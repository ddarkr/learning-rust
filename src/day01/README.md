## 1일차 러스트 학습

러스트는 PHP나 JS같은 인터프리터 (스크립트) 언어가 아닌 것 같다. 컴파일을 통해서 작동하는 듯.

### 컴파일 하기
```bash
rustc main.rs
```

### 실행
```bash
>> ./main
Hello, world!
```

> windows 환경에서는 `main.exe`를 실행해야 한다고 함.

그럭저럭 잘 되는듯. repl.it에서 실행해서 잘은 모르겠지만.